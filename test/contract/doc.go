// Package contract holds the L3 contract gates that run under `make contract`
// (build tag `contract`): the seeded Schema Registry FULL-compatibility check
// with its red fixture. The Pact gRPC and message-pact gates live in the
// sibling package test/pact (build tag `pact`); both run together under
// `make contract`.
package contract
